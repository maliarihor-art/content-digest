# Copy to .env and fill values. Never commit .env.

# Anthropic API key for the serverless digest proxy (api/digest.ts).
# Created/billed by the owner on console.anthropic.com; in production it is set
# as a Vercel project secret — never committed, never sent to the browser (ADR 003).
ANTHROPIC_API_KEY=
